import 'package:flutter/material.dart';

class HedethView extends StatelessWidget {
  const HedethView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'HedethView',
        style: TextStyle(fontSize: 30, fontWeight: FontWeight.bold),
      ),
    );
  }
}
