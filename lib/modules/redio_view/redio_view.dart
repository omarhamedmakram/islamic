import 'package:flutter/material.dart';

class RedioView extends StatelessWidget {
  const RedioView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'RedioView',
        style: TextStyle(fontSize: 30, fontWeight: FontWeight.bold),
      ),
    );
  }
}
